[project]
name = "corp-ca"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["certifi", "cryptography", "requests"]

[project.scripts]
corp-ca = "corp_ca:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
